#ifndef CCDDE_H
#define CCDDE_H
#include "td_platform.h"
/* CCDDE (Windows DDE) is not used in the Android port. 
   DDEServerClass is stubbed in td_platform.h. */
extern DDEServerClass DDEServer;
#endif
